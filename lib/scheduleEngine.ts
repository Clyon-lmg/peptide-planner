export function isDoseDay(d:Date,item:any){const dow=d.getDay();if(item.schedule==='EVERYDAY')return true;if(item.schedule==='WEEKDAYS')return dow>=1&&dow<=5;if(item.schedule==='CUSTOM'){const s=new Set(item.custom_days||[]);return s.has(dow);}return false;}export function monthGrid(cur:Date){const first=new Date(cur.getFullYear(),cur.getMonth(),1);const start=new Date(first);start.setDate(first.getDate()-first.getDay());const days=[];for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);days.push(d);}return days;}